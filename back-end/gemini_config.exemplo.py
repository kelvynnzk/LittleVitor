# ============================================================
# MODELO de configuração do chatbot (Google Gemini) — NÃO é usado
# diretamente.
# ============================================================
# 1. Copie este arquivo e renomeie a cópia para "gemini_config.py"
#    (esse nome já está no .gitignore, então nunca vai parar no Git).
# 2. Acesse https://aistudio.google.com/apikey (é grátis, não pede
#    cartão de crédito pra criar a chave gratuita).
# 3. Clique em "Create API key" e copie a chave gerada.
# 4. Cole abaixo.
#
# Essa chave É secreta — nunca cole ela no chat com a IA, nunca
# compartilhe esse arquivo, e nunca a coloque em nenhum HTML/JS do
# frontend.
# ============================================================

GEMINI_API_KEY = ""   # ex: "AIzaSy..."
